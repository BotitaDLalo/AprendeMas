import 'package:aprende_mas/config/utils/packages.dart';

class NoticesGroupOptionsScreen extends ConsumerWidget {
  const NoticesGroupOptionsScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}