import 'package:flutter/material.dart';

class CreateFileScreen extends StatelessWidget {
  const CreateFileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('subida de archivos'),
    );
  }
}