import 'package:flutter/material.dart';

class CreateExamScreen extends StatelessWidget {
  const CreateExamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('formulario de creación de examenes'),
    );
  }
}